name: arquict
description: Aplicando Clean Architeture.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.18.4 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  retrofit: ^4.1.0
  floor: ^1.4.2
  flutter_bloc: ^8.1.5
  equatable: ^2.0.5
  get_it: ^7.7.0
  flutter_hooks: ^0.20.5
  auto_route: ^8.0.3
  awesome_dio_interceptor: ^1.2.0
  oktoast: ^3.4.0
  ionicons: ^0.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.2
  lint: ^2.3.0
  auto_route_generator:
  retrofit_generator:
  floor_generator:
  build_runner:

flutter:
  uses-material-design: true